{
  "name": "lead-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Контур распределения лидов: Google Sheets -> очередь -> назначение -> вебхуки в Аргус",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/db/migrate.js",
    "test": "node --test \"test/*.test.js\"",
    "seed": "node src/db/seed.js",
    "check-sheet": "node scripts/check-sheet.js",
    "check-header": "node scripts/check-header.js"
  },
  "dependencies": {
    "google-auth-library": "^9.15.1"
  }
}
